# EchoForge NATS JetStream configuration
# Mount: ./nats.conf:/etc/nats/nats.conf:ro
# Start: nats-server -config /etc/nats/nats.conf

server_name: echoforge

jetstream {
  store_dir: /data/jetstream
  max_mem_store: 1GB
  max_file_store: 20GB
}

# Monitoring endpoint
http: 8222
